import { colors } from './colors';
import { spacing } from './spacing';
import { fontSize, fontWeight } from './typography';
import { radius } from './radius';
export { LIST, EMPTY_STATE, SWIPE, FOOD_NAME, SEARCH, PRESS } from './constants';

export const theme = {
  colors,
  spacing,
  fontSize,
  fontWeight,
  radius,
};

export { colors, spacing, fontSize, fontWeight, radius };