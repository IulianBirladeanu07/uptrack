import { StyleSheet } from 'react-native';
import { normalize } from '../../../../shared/hooks/useResponsive';

export const COLORS = {
  bg: '#0A0E13',
  surface: '#151B23',
  surfaceLight: '#1a1d24',
  primary: '#FF9500',
  primaryDark: '#E68600',
  success: '#32D74B',
  warning: '#FF9F0A',
  purple: '#9333EA',
  cyan: '#00d4ff',
  cyanFaded: 'rgba(6, 182, 212, 0.15)',
  cyanBorder: 'rgba(6, 182, 212, 0.3)',
  cyanFadedDark: 'rgba(6, 182, 212, 0.08)',
  cyanBorderDark: 'rgba(6, 182, 212, 0.25)',
  primaryFaded: 'rgba(255, 149, 0, 0.08)',
  primaryBorder: 'rgba(255, 149, 0, 0.2)',
  textPrimary: '#F9FAFB',
  textSecondary: '#9CA3AF',
  textTertiary: '#6B7280',
  disabled: '#4a4d55',
  border: 'rgba(255, 255, 255, 0.08)',
  borderLight: 'rgba(255, 255, 255, 0.05)',
  overlay: 'rgba(0, 0, 0, 0.8)',
  overlayDark: 'rgba(0, 0, 0, 0.2)',
  surfaceFaded: 'rgba(255, 255, 255, 0.05)',
  surfaceFadedLight: 'rgba(255, 255, 255, 0.06)',
  surfaceFadedMedium: 'rgba(255, 255, 255, 0.1)',
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: COLORS.bg,
  },
  loadingContainer: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },

  headerStatsRow: {
    flexDirection: 'row',
    justifyContent: 'space-around',
    backgroundColor: COLORS.surface,
    borderRadius: normalize(14),
    paddingVertical: normalize(14),
    marginBottom: normalize(20),
    marginHorizontal: normalize(2),
    borderWidth: normalize(1),
    borderColor: COLORS.border,
  },
  headerStatItem: {
    alignItems: 'center',
  },
  headerStatIcon: {
    marginBottom: normalize(4),
  },
  headerStatValue: {
    fontSize: normalize(16),
    fontWeight: '700',
    color: COLORS.textPrimary,
    marginBottom: normalize(2),
  },
  headerStatLabel: {
    fontSize: normalize(11),
    color: COLORS.textSecondary,
    fontWeight: '500',
  },

  workoutCard: {
    backgroundColor: COLORS.surface,
    borderRadius: normalize(18),
    marginBottom: normalize(20),
    padding: normalize(18),
    borderWidth: normalize(1),
    borderColor: COLORS.border,
  },
  workoutCardHeader: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'flex-start',
    marginBottom: normalize(16),
  },
  workoutInfo: {
    flex: 1,
    paddingRight: normalize(14),
  },
  workoutTitleRow: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: normalize(8),
    marginBottom: normalize(12),
  },
  workoutCardTitle: {
    fontSize: normalize(20),
    fontWeight: '700',
    color: COLORS.textPrimary,
    lineHeight: normalize(24),
  },
  workoutMetrics: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    gap: normalize(12),
  },
  metricItem: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: normalize(4),
  },
  metricText: {
    fontSize: normalize(12),
    color: COLORS.textSecondary,
    fontWeight: '500',
  },
  previewButton: {
    backgroundColor: COLORS.cyanFaded,
    borderRadius: normalize(14),
    width: normalize(42),
    height: normalize(42),
    alignItems: 'center',
    justifyContent: 'center',
    borderWidth: normalize(1),
    borderColor: COLORS.cyanBorder,
  },

  exerciseGrid: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    gap: normalize(8),
    marginBottom: normalize(18),
  },
  exerciseGridItem: {
    backgroundColor: COLORS.surfaceFadedLight,
    borderRadius: normalize(12),
    paddingVertical: normalize(8),
    paddingHorizontal: normalize(12),
    borderWidth: normalize(1),
    borderColor: COLORS.border,
  },
  exerciseGridText: {
    color: COLORS.textSecondary,
    fontSize: normalize(13),
    fontWeight: '600',
  },
  exerciseGridMore: {
    color: COLORS.cyan,
    fontSize: normalize(12),
    fontWeight: '600',
  },

  startButtonContainer: {
    marginTop: normalize(6),
  },
  startButton: {
    backgroundColor: COLORS.primary,
    borderRadius: normalize(14),
    paddingVertical: normalize(14),
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    width: '100%',
    shadowColor: COLORS.primary,
    shadowOffset: { width: 0, height: normalize(4) },
    shadowOpacity: 0.3,
    shadowRadius: normalize(8),
    elevation: normalize(6),
  },
  startButtonDisabled: {
    backgroundColor: COLORS.disabled,
    shadowOpacity: 0,
    elevation: 0,
  },
  startButtonText: {
    color: COLORS.bg,
    fontSize: normalize(15),
    fontWeight: '700',
    letterSpacing: normalize(0.5),
  },
  playIcon: {
    color: COLORS.bg,
    marginRight: normalize(8),
  },

  restDayCard: {
    backgroundColor: COLORS.surface,
    borderRadius: normalize(18),
    marginBottom: normalize(20),
    padding: normalize(18),
    borderWidth: normalize(1),
    borderColor: COLORS.border,
  },
  restDayContent: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  restDayIconContainer: {
    width: normalize(56),
    height: normalize(56),
    borderRadius: normalize(28),
    backgroundColor: COLORS.cyanFaded,
    alignItems: 'center',
    justifyContent: 'center',
    marginRight: normalize(14),
    borderWidth: normalize(1.5),
    borderColor: COLORS.cyanBorder,
  },
  restDayText: {
    flex: 1,
  },
  restDayTitle: {
    fontSize: normalize(20),
    fontWeight: '700',
    color: COLORS.textPrimary,
    marginBottom: normalize(4),
  },
  restDaySubtitle: {
    fontSize: normalize(13),
    color: COLORS.textSecondary,
    lineHeight: normalize(18),
  },

  progressCard: {
    backgroundColor: COLORS.surface,
    borderRadius: normalize(18),
    padding: normalize(18),
    marginBottom: normalize(20),
    borderWidth: normalize(1),
    borderColor: COLORS.border,
  },
  progressHeader: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    marginBottom: normalize(12),
  },
  sectionTitle: {
    fontSize: normalize(17),
    fontWeight: '700',
    color: COLORS.textPrimary,
  },
  progressSubtitle: {
    fontSize: normalize(13),
    color: COLORS.textSecondary,
    fontWeight: '500',
  },
  daysContainer: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
  },
  dayCircle: {
    width: normalize(38),
    height: normalize(38),
    borderRadius: normalize(19),
    alignItems: 'center',
    justifyContent: 'center',
  },
  activeDayCircle: {
    backgroundColor: COLORS.primary,
  },
  inactiveDayCircle: {
    backgroundColor: COLORS.surfaceFadedMedium,
    borderWidth: normalize(1),
    borderColor: COLORS.border,
  },
  dayText: {
    fontSize: normalize(14),
    fontWeight: '600',
    color: COLORS.bg,
  },
  inactiveDayText: {
    color: COLORS.textTertiary,
  },

  mergedCard: {
    backgroundColor: COLORS.surface,
    borderRadius: normalize(18),
    padding: normalize(18),
    marginBottom: normalize(20),
    borderWidth: normalize(1),
    borderColor: COLORS.border,
  },
  sectionHeader: {
    marginBottom: normalize(14),
  },
  upcomingGrid: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    gap: normalize(10),
  },
  upcomingGridItem: {
    flex: 1,
    minWidth: '48%',
    maxWidth: '48%',
    backgroundColor: COLORS.surfaceFaded,
    borderRadius: normalize(14),
    padding: normalize(14),
    borderWidth: normalize(1),
    borderColor: COLORS.border,
    minHeight: normalize(85),
  },
  upcomingGridItemRest: {
    backgroundColor: COLORS.cyanFadedDark,
    borderColor: COLORS.cyanBorderDark,
  },
  upcomingGridHeader: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    marginBottom: normalize(8),
  },
  upcomingGridDay: {
    fontSize: normalize(12),
    fontWeight: '700',
    color: COLORS.textSecondary,
    textTransform: 'uppercase',
    letterSpacing: normalize(0.5),
  },
  upcomingGridName: {
    fontSize: normalize(15),
    fontWeight: '700',
    color: COLORS.textPrimary,
    marginBottom: normalize(4),
    lineHeight: normalize(19),
  },
  upcomingGridNameRest: {
    color: COLORS.cyan,
  },
  upcomingGridInfo: {
    fontSize: normalize(12),
    color: COLORS.textSecondary,
    fontWeight: '500',
    lineHeight: normalize(16),
  },

  quickActionsContainer: {
    flexDirection: 'row',
    gap: normalize(8),
    marginBottom: normalize(20),
  },
  quickActionButton: {
    flex: 1,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: COLORS.surface,
    borderRadius: normalize(12),
    paddingVertical: normalize(14),
    paddingHorizontal: normalize(10),
    gap: normalize(8),
    borderWidth: normalize(1),
    borderColor: COLORS.border,
  },
  quickActionButtonText: {
    fontSize: normalize(14),
    fontWeight: '600',
    color: COLORS.textPrimary,
  },

  modalOverlay: {
    flex: 1,
    backgroundColor: COLORS.overlay,
    justifyContent: 'center',
    alignItems: 'center',
    paddingHorizontal: normalize(18),
  },
  modalContent: {
    backgroundColor: COLORS.surface,
    borderRadius: normalize(18),
    width: '100%',
    maxHeight: '80%',
    borderWidth: normalize(1),
    borderColor: COLORS.border,
  },
  modalHeader: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    padding: normalize(18),
    paddingBottom: normalize(14),
    borderBottomWidth: normalize(1),
    borderBottomColor: COLORS.borderLight,
  },
  modalTitle: {
    fontSize: normalize(19),
    fontWeight: '700',
    color: COLORS.textPrimary,
  },
  modalCloseButton: {
    backgroundColor: COLORS.surfaceFadedMedium,
    borderRadius: normalize(12),
    width: normalize(32),
    height: normalize(32),
    alignItems: 'center',
    justifyContent: 'center',
  },
  modalScrollView: {
    paddingHorizontal: normalize(18),
    paddingVertical: normalize(14),
  },
  modalStatsRow: {
    flexDirection: 'row',
    justifyContent: 'space-evenly',
    alignItems: 'center',
    paddingVertical: normalize(12),
    marginBottom: normalize(12),
    borderBottomWidth: normalize(1),
    borderBottomColor: COLORS.borderLight,
  },
  modalStatItem: {
    alignItems: 'center',
  },
  modalStatValue: {
    fontSize: normalize(18),
    fontWeight: '800',
    color: COLORS.primary,
  },
  modalStatLabel: {
    fontSize: normalize(8),
    fontWeight: '700',
    color: COLORS.textTertiary,
    textTransform: 'uppercase',
    letterSpacing: 1,
    marginTop: normalize(2),
  },
  modalStatDivider: {
    width: 1,
    height: normalize(25),
    backgroundColor: COLORS.border,
  },
  modalExercisesList: {
    backgroundColor: COLORS.overlayDark,
    borderRadius: normalize(12),
    paddingVertical: normalize(6),
    borderWidth: normalize(1),
    borderColor: COLORS.borderLight,
  },
  modalExerciseRow: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    paddingVertical: normalize(10),
    paddingHorizontal: normalize(14),
  },
  modalExerciseRowLast: {
    borderBottomWidth: 0,
  },
  modalExerciseLeft: {
    flex: 1,
    flexDirection: 'row',
    alignItems: 'center',
    marginRight: normalize(12),
  },
  modalExerciseSets: {
    fontSize: normalize(12),
    fontWeight: '700',
    color: COLORS.primary,
    marginRight: normalize(8),
  },
  modalExerciseName: {
    flex: 1,
    fontSize: normalize(13),
    fontWeight: '600',
    color: COLORS.textPrimary,
    letterSpacing: 0.2,
  },
  modalBestSetContainer: {
    alignItems: 'flex-end',
  },
  modalBestSetValue: {
    fontSize: normalize(12),
    fontWeight: '600',
    color: COLORS.textSecondary,
    letterSpacing: 0.3,
  },
  modalStartButton: {
    backgroundColor: COLORS.primary,
    borderRadius: normalize(14),
    paddingVertical: normalize(14),
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    margin: normalize(18),
    marginTop: normalize(14),
  },
  modalStartButtonDisabled: {
    backgroundColor: COLORS.disabled,
    shadowOpacity: 0,
    elevation: 0,
  },
  modalStartButtonText: {
    color: COLORS.bg,
    fontSize: normalize(15),
    fontWeight: '700',
    letterSpacing: normalize(0.5),
  },
  compactBanner: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    backgroundColor: COLORS.surfaceLight,
    paddingVertical: normalize(10),
    paddingHorizontal: normalize(14),
    borderRadius: normalize(12),
    marginBottom: normalize(12),
    borderWidth: 1,
    borderColor: COLORS.primary,
  },
  bannerLeft: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: normalize(8),
  },
  pulseDot: {
    width: normalize(8),
    height: normalize(8),
    borderRadius: normalize(4),
    backgroundColor: COLORS.primary,
  },
  bannerText: {
    color: COLORS.textPrimary,
    fontSize: normalize(13),
    fontWeight: '600',
  },
  bannerExercises: {
    color: COLORS.textSecondary,
    fontSize: normalize(11),
  },
  bannerRight: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: normalize(8),
  },
  compactTimerText: {
    color: COLORS.primary,
    fontSize: normalize(13),
    fontWeight: '600',
  },
});

export default styles;